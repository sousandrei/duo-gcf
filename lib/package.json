{
	"name": "public-server",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"babel": "babel *.js -d lib/",
		"deploy-local-babel": "(cd lib/ && functions deploy duo --trigger-http)",
		"deploy-local": "functions deploy duo --trigger-http",
		"debug-babel": "npm run babel && npm run deploy-local-babel && functions inspect duo",
		"debug": "npm run deploy-local && functions inspect duo"
	},
	"dependencies": {
		"dotenv": "^4.0.0",
		"mongoose": "^4.12.1"
	},
	"babel": {
		"plugins": [
			"transform-async-to-generator"
		],
		"presets": [
			"node6"
		]
	},
	"devDependencies": {
		"babel-cli": "^6.26.0",
		"babel-plugin-transform-async-to-generator": "^6.24.1",
		"babel-preset-node6": "^11.0.0"
	}
}
